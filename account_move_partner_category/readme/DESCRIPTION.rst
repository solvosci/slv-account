Adds partner categories for invoices, so it's possible to filter
and group by them.

Groping is achieved with partner categories concatenation, because it's not
possible to group by ``Many2many`` fields.
