
<odoo>

    <record id="view_account_move_form" model="ir.ui.view">
        <field name="name">Account Move (ATM)</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//h1/field[@name='name']/.." position="after">
                <field name="encrypted_name_lower" invisible="not atm_invoice" groups="base.group_no_one"/>
            </xpath>
            <xpath expr="//group[@id='header_right_group']/field[@name='currency_id']" position="after">
                <field name="atm_invoice" groups="base.group_no_one"/>
            </xpath>
        </field>
    </record>

</odoo>
